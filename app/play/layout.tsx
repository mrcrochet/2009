import type { ReactNode } from 'react'
import './play.css'

export const metadata = {
  title: 'UNLISTED — NOVA 3.2',
}

export default function PlayLayout({ children }: { children: ReactNode }) {
  return children
}
