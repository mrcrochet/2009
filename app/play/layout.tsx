import type { ReactNode } from 'react'
import './play.css'

export const metadata = {
  title: '2009 — HALCYON 4.1',
}

export default function PlayLayout({ children }: { children: ReactNode }) {
  return children
}
